DOMAIN=s3.anri.cloud            # domain name
MINIO_ROOT_USER=anri            # minio root user
MINIO_ROOT_PASSWORD=anri-minio  # minio root passwrod
MINIO_BROWSER_REDIRECT_URL=https://${DOMAIN}/console

# Define name of your cert here, there is store in ssl folder
SSL_CERT=server.pem             # server cert
SSL_KEY=server.pem              # server key